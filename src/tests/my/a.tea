struct Node {
    l:int,
    r:int
}

fn baz(c[10]:Node) {
    c[2].l = getint();
    //c[2].r = 4;
    ret;
}

let d:Node;
let e[10]:Node;

fn main()->int{
    _sysy_starttime(13);
    baz(e);
    putint(e[2].l);
    _sysy_stoptime(19);
    ret 0;
}